{
  "name": "cxr-ai-analyzer",
  "private": true,
  "scripts": {
    "dev:all": "concurrently -k -n api,ui -c cyan,green \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": ".venv\\Scripts\\python -m uvicorn api.main:app --reload --host 127.0.0.1 --port 8001",
    "dev:ui": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
